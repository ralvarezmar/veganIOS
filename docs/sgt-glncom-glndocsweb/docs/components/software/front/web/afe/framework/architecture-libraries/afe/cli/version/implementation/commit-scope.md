# Adding a scope to a commit

You can add a scope to your commit, so that you can organize what the change is about

``` BASH
git commit -m "fix(http): Fixing requests behavior"
```

Resulting in:

``` MD
## 5.7.1 (2020-02-06)

### Fixes

* **http:** Fixing requests behavior
```
