# Darwin-spring-boot-webservice Migration guides

## Version 3.0.0-RELEASE

<!tag:300>

Packages refactoring from `es.santander.darwin` to `com.santander.darwin`.

<!end:300>

## Version 2.4.0-Release

<!tag:240>

- The root of the library becomes `es.santander.darwin.ws` instead of `es.santander.darwin.interceptor`.

- Service class has been renamed `EndpointSelectorService` to `WebEndpointSelectorService`.

- The constant is removed **InterceptorsFactory.USER\_NAME\_VALID\_VALUE**.

<!end:240>
